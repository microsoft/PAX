<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow outgoing network connections (for Microsoft Graph API) -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Allow spawning external processes (for PowerShell execution) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    
    <!-- Allow file system access (for reading/writing export files) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    
    <!-- Allow access to user's home directory -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
    
    <!-- Allow JIT compilation (might be needed for JavaScript engine) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Allow unsigned executable memory (for dynamic code) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Allow loading arbitrary libraries (for PowerShell modules) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>

